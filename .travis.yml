language: bash
services: docker

install:
  - docker build --tag=lnintegration .
  - docker run --name lnintegration lnintegration > log.txt ; echo $? > result.txt
script:
  - echo "result: " && cat result.txt
